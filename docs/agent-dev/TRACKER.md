# TRACKER.md — 결과 트래커

CI가 없으므로, 이 로그가 "누가 뭘 했는지"를 공유하는 유일한 장치입니다. **커밋할 때마다** (최소한
세션을 끝낼 때) 아래 표에 한 줄을 추가하세요. 정기 회의는 이 표를 보고 시작합니다 — 여기 없는
변경사항은 회의에서 논의되지 않을 수 있습니다.

작성 규칙:
- 최신 항목이 위로 오게 추가하세요 (역시간순).
- `요약`은 diff를 안 봐도 무슨 변경인지 알 수 있게 한 줄로 (커밋 메시지 재탕이어도 무방).
- `관련 문서`에는 해당 작업으로 갱신된 CONTRACTS.md 버전이나 새로 등재/해소된 DECISIONS.md 항목 ID를 적으세요.

| 날짜 | 커밋 해시 | 작성자 (도구) | Phase | 요약 | 관련 문서 |
|---|---|---|---|---|---|
| 2026-08-11 | `705fea4` | 팀장 imtmtl (Claude Code) | ④ 스킬/에셋 | **claw 조준 방식 비교용 대전 봇 2종 추가** (`claw_landing_point_bot.js` / `claw_opponent_position_bot.js`). "어디에 쏠 것인가"만 다르게 하려고 비스킬 로직을 **주석 뺀 실행 코드 기준 한 줄 차이**(`aimX()`의 return)로 맞췄고, 발동 타이밍까지 공유시켰다 — 조준과 타이밍이 같이 달라지면 비교가 성립하지 않기 때문. 1번은 **낙하지점 리드 사격**(claw가 터지는 데 25프레임 걸리므로 '지금 선 자리'는 1초 뒤 빈 자리다. 공의 남은 비행시간이 그 1초와 맞는 순간에만 발동해 상대가 리시브하러 반드시 서야 하는 자리와 일치시킴), 2번은 `opp.x` 그대로 조준하는 대조군. **비스킬 플레이는 기본 AI(`letComputerDecideUserInput`)를 근거로 삼되 세 군데 조정**: (1) 점프 조건 — `ball.y < self.y-20`은 사실상 항상 참이라 헛점프를 낳음 → AI의 `ball.y < 10*boldness+84` + x정렬 32px. (2) 스매시 방향 — `expectedLandingPointXWhenPowerHit()`를 이식해 6개 조합 착지점을 시뮬레이션(두 입력은 방향을 조종하지 않는다: x는 속도 `(\|x\|+1)*10`, y는 수직속도 2배·부호). 단 AI처럼 '상대와 64px 이상'이 안 맞을 때 포기하지 않고 상대 코트에만 떨어지면 치도록 완화 — **원작엔 없던 5회 터치 제한 때문에 안 치고 버티면 그대로 실점**. (3) 스윙 타이밍 — 사거리 48px 창을 기다리지 않고 점프 내내 `hit=1` 홀드. powerHit은 충돌 프레임의 `playerState===2`로만 스매시가 되고 state 2는 ~10프레임 뒤 state 1로 돌아가 홀드하면 즉시 재진입하는데(`physics.js:608-615`), 창은 공이 한 틱이면 지나가는 폭인데 스냅샷은 한 틱 낡아 기다리면 놓친다(계측: 공중 267프레임 중 사거리 안 42프레임). **검증**: botWorker와 동일한 `new Function`으로 컴파일해 로직 31케이스 / Playwright 실제 대전 — 스킬 끈 조건 양 진영 14:1·10:5, 헛점프 0%(대조군 18~32%), 터치 제한 실점 0회(수정 전 판당 4회), 게이지 채운 조건 발톱 명중률 1번 49발 49명중(100%) vs 2번 40발 32명중(80%), 콘솔 에러 0 | CONTRACTS.md v0.7 준수(변경 없음). **회의 안건 2건**: ① 스텁 수치(cost 50, onReceive 10)로는 봇 대전 90초에 게이지 최대 20~40이라 claw가 사실상 발동 불가 — 조준 비교에 게이지 강제충전이 필요했음. ② 잘 조준한 claw는 회피가 거의 불가(1번 49발 무실패) — 안전거리 80px를 25프레임 안에 벗어나야 하는데 낙하지점을 노리면 피격자가 점수를 포기하지 않는 한 그 안으로 걸어 들어옴. `04-bot-writing-guide.md` 등재는 미완 |
| 2026-08-11 | `2bffabd` | 팀장 imtmtl (Claude Code) | ④ 스킬/에셋 | **`develop` → `develop-skill` 병합** (운영자 콘솔·5회 터치 제한·서브권 랜덤/승자/패자·pyodide 빌드 수정 통합). AGENTS.md §5가 요구하는 정기 병합. 충돌 3건 해소: AGENTS.md §5는 양쪽이 같은 문단을 같은 취지로 고쳐 날짜만 달랐고(develop의 `4a2ada1`이 `bf2e31a`와 중복 작업) develop 쪽 채택, TRACKER.md는 표 상단 append 충돌이라 양쪽 행 전부 유지, `main.js` `setup()`은 겹치는 로직 없이 각자 다른 배선을 추가한 것이라 셋 다 살렸으나 **start() 뒤 ticker 콜백 등록 순서**는 자동 병합이 못 정한다 → `skills.startObserving`을 `setUpTouchLimit`보다 **먼저** 등록. 터치 제한이 먼저 돌면 `awardPoint()` → `forceNextRound()`가 `roundEnded`·`state`를 동기적으로 바꿔(`operator/console.js:142-153`) 5번째 접촉 프레임에서 이미 `isRallyLive()`가 false가 되어 **그 접촉의 게이지가 차지 않는다**. 제한을 유발한 터치도 랠리 중 접촉이므로 게이지는 얻고 나서 랠리가 닫히는 쪽이 ADR-0024 취지에 맞다고 판단. **밸런스 판단이 섞인 결정이라 회의에서 확정할 것.** 검증: `npm run verify` 통과(lint·build 에러 0, 남은 경고 5건은 pyodide wasm·에셋 크기 등 기존 경고) | ADR-0024 관련. **후속(미착수)**: `rules/touchLimit.js:94`가 랠리 종료를 `state !== round`로 보고 있음 — ADR-0024가 틀렸다고 정리한 그 게이트라 `isRallyLive()`로 통일 필요 |
| 2026-08-11 | `7b1722b` | 팀장 imtmtl (Claude Code) | ④ 스킬/에셋 | **랠리 경계 판정 정정 — 공이 떨어진 뒤에도 게이지가 차던 버그 수정**(팀장 제보). 원인은 랠리 종료를 `state !== round`로 본 것. `round()`는 착지 후에도 계속 돈다: 착지 시 `roundEnded=true` + 슬로모션 6프레임이 걸리고(`pikavolley.js:396-398`) `afterEndOfRound`로 넘어가는 건 그 프레임을 다 쓴 뒤(`:403-405`)라, `slowMotionFPS=5` 기준 **체감 약 1.2초** 동안 `runEngineForNextFrame`(`:345`)이 계속 돌며 접촉이 잡혔다. 같은 뿌리로 **claw도 동일 게이트를 복사**해 써서 점수 확정 후 1.2초 안에 발동·명중이 가능했고(CONTRACTS §1.1은 이미 "랠리 밖 발동 실패"라고 규정 → 문서가 맞고 코드가 틀린 케이스), `runEngineForNextFrame`이 `gameEnded` early-return(`:353-365`)보다 위에 있어 **승패 메시지 동안에도** 게이지가 찼다. 신규 `skill/rally.js`의 `isRallyLive()` = `state===round && !roundEnded && !gameEnded` 하나로 판정을 모으고 `gauge.js observe`·`claw.js tryCast`·`claw.js observe` 세 곳이 이걸 쓰게 함(각자 판정하다 한쪽만 고쳐지는 것 방지, 이후 스킬도 이걸 쓸 것). 밸런스 수치·스냅샷 필드는 무변경이라 **CONTRACTS 버전은 v0.7 유지**(VERIFY §4 기준 계약 변경이 아니라 계약 준수). 연습 모드는 `roundEnded`가 세팅되지 않아 영향 없음(`:367-372`). **검증**: `npm run verify` 통과 / 실제 모듈 + 가짜 엔진으로 16케이스(진리표 4, 착지 후·경기종료 중 미충전, 다음 랠리 첫 접촉=서브 유지, 연습 모드 정상 충전, claw 발동 실패·게이지 미소모·예고 취소) — **수정 전 코드로 돌리면 6건 실패**하는 것까지 확인해 테스트가 실제로 버그를 잡는지 검증 / Playwright로 기본AI 대전을 프레임마다 계측(`window.__pikaVolley`+`__pikaSkills`) — 수정 후 랠리 중 충전 21회·죽은 구간 123틱에서 충전 **0회**, **수정 전 빌드로는 같은 계측에서 `roundEnded=true` 중 게이지 35→45 누수 1건 재현**, 콘솔 에러 0 | ADR-0024 신규(RESOLVED, 회의 승인 전), ADR-0020 §구현에 정정 노트 추가(랠리 경계 가정이 틀렸음), DECISIONS.md 인덱스. CONTRACTS.md 변경 없음(v0.7 유지). Issue #14 |
| 2026-08-11 | (커밋 전) | 팀장 imtmtl (Claude Code) | ④ 스킬/에셋 | **스냅샷 계약 검사용 예제 봇 추가** (`docs/agent-dev/example-bots/skill_contract_check_bot.js`). 전략용이 아니라 검증용으로, 매 틱 CONTRACTS.md §1.2/§1.2.1의 모든 필드를 타입·범위까지 검사하고 틱 사이 변화(예고 카운트다운 감소, 예고 중 `centerX` 고정, 범위 안 피격 시 `state===4`, 게이지 증감폭)까지 본다. 위반은 `[CONTRACT FAIL]`로 같은 메시지당 한 번만 출력하고 ~3초마다 요약(`ERRORS=0` 유지 여부만 보면 됨). 검사하면서 회피·발동도 실제로 하므로 양쪽에 붙이면 스킬 경로가 전부 지나간다. 3프레임 샘플링 오차와 경기 리셋 구간은 오탐이 안 나도록 여유를 뒀다. 04번 가이드에 §6.5로 등재. **CI가 없는 상태에서 스냅샷 API 회귀를 손으로 잡을 수 있는 유일한 수단**이라 밸런스 수치를 바꿀 때마다 한 번씩 돌려볼 것 | `docs/architecture/04-bot-writing-guide.md` §6.5. `00116ab`(Phase 4-B-2) 후속. Issue #12 |
| 2026-08-11 | `00116ab` | 팀장 imtmtl (Claude Code) | ④ 스킬/에셋 | **Phase 4-B-2 스냅샷 확장 — 봇이 드디어 발톱을 피할 수 있게 됨.** 스냅샷에 (1) 플레이어별 `gauge`(0~100, 상대 것도), (2) 플레이어별 `claw`(**시전자 기준**이라 `opp.claw`가 나를 노리는 것 / `self.claw`는 내 슬롯 점유 여부. `centerX`·`framesUntilStrike`·`framesLeftActive`), (3) `lyingDownDurationLeft`(기절 잔여, 해제까지 `값+2` 프레임), (4) `config.gauge`/`config.claw` **튜닝 상수 블록**을 추가. 상수를 실어 보내는 이유는 이 숫자들이 전부 stub이라 봇이 하드코딩하면 회의에서 값을 바꾸는 순간 **에러 없이 조용히 틀린 판단**을 하기 때문(D-023 §3). "맞는지" 여부를 미리 계산한 불리언은 일부러 안 줌 — 회피 판단 자체가 이 스킬의 전략이라 재료(`centerX`/`width`/`PLAYER_HALF_LENGTH`)만 제공. **배선**: 스냅샷 빌더가 트래커를 읽어야 하는데 기존 `main.js`는 트래커를 봇 UI보다 뒤에 만들고 있었음 → `skill/setup.js`를 `setUpSkills()` 하나로 합치고 **생성(트래커·뷰·키 리스너)** 과 **관찰 배선(`startObserving(ticker)`)** 을 분리, 생성은 봇 UI 앞·ticker 등록은 `start()` 뒤로 두어 "관찰은 gameLoop 뒤" 순서를 유지(콜백 내부 실행 순서도 기존 두 콜백과 동일). 전역 싱글턴 대신 `getMeta`와 같은 **주입** 방식(`main.js`→`testSetup`→`PikaBotInput`). 예제 봇(JS/Python 둘 다)에 회피·발동 최소 구현 추가. 자동화용으로 `window.__pikaSkills` 노출(`window.__pikaVolley`와 같은 취지). **검증**: `npm run verify` 통과 / 실제 모듈 + 가짜 엔진으로 로직 42케이스(자기·상대 매핑 양쪽 side, 스텁 경로, config 값, 카운트다운→피격→기절→슬롯 해제 e2e, **문서에 적은 피격 공식이 실제 `isWithinClaw()`와 0~432 전 픽셀에서 일치**하는지, 스냅샷만 보고 회피 성공) / Playwright로 **봇 vs 봇** 실제 대전 — Worker 안에서 봇이 받은 스냅샷을 122건 캡처해 `config` 값·게이지 변화·`opp.claw` 카운트다운(21→11→0)·기절 중 `lyingDownDurationLeft=19` 확인, 봇 자율 발동 43회 / 피격 판정 34회 중 **17회 회피 성공**(확장 전에는 회피 확률 0), 콘솔 에러 0 / 예제 봇은 JS·Python을 **동일한 9케이스**(발동·게이지 부족·슬롯 점유·좌우 회피·먼 발톱 무시·터진 뒤 미회피·v0.6 스냅샷 폴백·side별 조준)로 각각 실행해 포팅 동등성 확인 | CONTRACTS.md **v0.7**(§1.2 갱신, §1.2.1 신설), ADR-0023 신규(RESOLVED, 회의 승인 전), ADR-0020 OPEN-3·ADR-0021 OPEN-1 해소 표기, DECISIONS.md 인덱스, PHASES.md Phase 4-B-2 완료, `docs/architecture/03-bot-integration.md`·`04-bot-writing-guide.md`(§4.1/§4.2/§4.4 신설). Issue #12 |
| 2026-08-11 | (커밋 전) | 팀장 imtmtl (Claude Code) | ④ 스킬/에셋 | **claw 스킬 1차 플레이 피드백 반영 3건.** (1) **판정·표시 범위를 x범위 안 모든 y로 확정** — `CLAW_HEIGHT` 64→304(코트 전체), 예고/발톱 기둥이 화면 전체 높이로 그려져 판정 범위와 표시가 일치. (2) **임시 이미지 반투명화** — 384×256 불투명 → 384×1216 반투명(예고 alpha 90, 발톱 140). 불투명하면 발톱이 피격자를 가려서 "제대로 땅에 떨어지는지" 확인이 불가능했음. (3) **봇이 발톱 x를 직접 지정** — 액션에 선택 필드 `skillX` 추가(D-022). 숫자면 그 x 중심으로 발동, 생략/null이면 미발동, 0~432 클램프, 숫자가 아니면 발동만 취소하고 x/y/hit은 정상 적용, 응답 1건당 1회만 소비(`PikaBotInput.consumeSkillX()`). 키보드는 기존대로 상대 위치 중심 유지. **CONTRACTS.md를 v0.6으로 먼저 올린 뒤 코드 수정**(VERIFY §4 절차) — §1.1에 필드/규칙 추가. 참가자 가이드(04)에도 선택 필드로 문서화. **스냅샷 확장(게이지·예고 중 발톱 노출)은 팀장 요청으로 이번 커밋에서 제외** → 현재 봇은 발동은 가능하나 회피는 구조적으로 불가. 검증: `npm run verify` 통과 / 로직 39케이스 통과(기존 26 + 봇 조준·클램프·`skillX` 파싱 13) / Playwright로 Bot Setup에 JS 봇(왼쪽) vs 기본 AI(오른쪽)를 실제 로드해 봇이 `skillX: 120`으로 발동시키고, 캔버스에서 발톱 기둥이 x≈72~153(기대 중심 120)에 그려지는 것과 상대 위치가 아닌 지정 좌표에 생기는 것을 실측, 콘솔 에러 0 | CONTRACTS.md **v0.6**, ADR-0022 신규(RESOLVED), ADR-0021 갱신(조준 규칙·판정 축 확정 → PARTIAL), DECISIONS.md 인덱스, PHASES.md Phase 4-B, `docs/architecture/04-bot-writing-guide.md` §4.3. Issue #10 |
| 2026-08-11 | (커밋 전) | 팀장 imtmtl (Claude Code) | ④ 스킬/에셋 | **Phase 4-B 첫 스킬 「claw」 1차 구현** (키보드 전용). 게이지 50 소모 → 상대 x 위치 중심으로 폭 96px 범위 예고 1초 → 발톱 생성 → 그 순간 범위 안 상대 1초 기절, 공중이면 즉시 착지. 신규 `skill/claw.js`(순수 로직 + 튜닝 상수 7개)·`skill/clawView.js`(예고/발톱 스프라이트), `gauge.js`에 `trySpend()` 추가, `setup.js`에 `setUpClaw()`(keydown 리스너 + ticker 배선), `assets_path.js`/`main.js`에 이미지 2개 로드, 임시 단색 PNG(`assets/images/skill/claw_warning.png`, `claw.png`, 384×256 = 물리 96×64 × RATIO 4) 생성. **엔진 무수정 유지(Tier A)** — 기절은 원작의 누움 상태(`physics.js:507-512`, `state=4` + `lyingDownDurationLeft`)를 밖에서 세팅하는 방식이라 이동 입력 무시·자동 복귀가 공짜로 따라오고 기본 AI에게도 동일하게 걸림. 공중 피격 시 순간이동은 스펙이자 필수 처리(누움 분기가 중력보다 먼저 return해서 안 그러면 허공에 정지). 발동 키는 `PikaKeyboard`가 아니라 별도 `keydown` 리스너로 받음(스킬 입력은 엔진 3필드 프로토콜 밖이라 키보드 클래스를 확장하면 봇/AI 슬롯까지 의미가 번짐). 검증: `npm run verify` 통과 / Playwright로 기본AI 대전 중 실제 발동 — 게이지 50→0, 예고 약 1초 표시 후 발톱 스프라이트가 정확히 96×64(물리) 크기로 출현, 콘솔 에러 0 / 발톱이 피격자를 가려 화면으로 확인 불가한 기절 판정은 실제 모듈 + 가짜 엔진 객체로 26개 케이스 검증(게이지 부족·중복 발동·기절 중 발동·랠리 종료 취소·예고 중 미발동·회피 성공·공중 피격 좌표 스냅·히트박스 경계·발톱 자동 소멸) | ADR-0021 신규(OPEN — 콘셉트 확정, 소모량/범위 폭/조준 규칙/발동키는 stub), DECISIONS.md 인덱스, PHASES.md Phase 4-B를 B-1(완료)·B-2(봇 프로토콜, 미착수)로 분해. CONTRACTS.md 변경 없음(봇 프로토콜 미변경). 폐기: 이전 검토안 「백만볼트」 |
| 2026-08-11 | (커밋 전) | 팀장 imtmtl (Claude Code) | ④ 스킬/에셋 | **게이지 시스템을 `develop-skill`에 통합**. `feature/5-gauge-system`(develop 분기 이전 상태)의 `skill/gauge.js`·`gaugeView.js`·`setup.js` + `main.js` 2줄을 가져오면서, 그 사이 develop에 들어온 RATIO 배선/펭수배구 스프라이트에 맞춰 렌더링을 두 군데 고침: (1) 막대 좌표·크기에 `ASSETS_PATH.RATIO`를 곱하도록 수정(안 하면 다른 요소만 4배가 되고 게이지바만 1/4로 남음), (2) **색을 스프라이트시트에서 떼어내 tint로 전환** — 새 시트에는 테두리용 `objects/black.png` 프레임이 아예 없고 `ground_red`/`ground_yellow`는 둘 다 코트색 계열(측정값 rgb(219,230,149))로 바뀌어 채움/빈 구간이 육안으로 구분되지 않았음. `Texture.WHITE`+tint 방식(`FadeInOut`과 동일)으로 교체하니 `GaugeView`가 loader resources를 안 쓰게 되어 `setUpGauge(pikaVolley, ticker)`로 시그니처 축소. 충전 규칙·관찰 로직(`GaugeTracker`)은 무변경. ADR 번호는 RATIO 결정이 0019를 선점해 **0019→0020으로 개명**(소스 주석 `D-019`→`D-020` 포함). 검증: `npm run verify` 통과, Playwright로 기본AI vs 기본AI 자동 랠리를 돌리며 캔버스 픽셀에서 막대 채움 길이를 실측 — 경기 시작 시 0, 이후 5/10 게이지 단위(3.3px/6.4px)로 증감, 양쪽 막대가 네트 쪽으로 채워지는 것, 콘솔 에러 0 확인. 원본 커밋 이력(`35e1360`, `0c776cf`, `0381437`)은 `feature/5-gauge-system` 브랜치에 남아 있음 | ADR-0020(구 ADR-0019, PARTIAL — 충전 규칙만 확정, 스킬 효과·소모량·봇 프로토콜·발동키는 OPEN), DECISIONS.md 인덱스, PHASES.md Phase 4 A/B/C 분해 + `develop-skill` 작업 브랜치 명시, `docs/architecture/05-skill-extension-guide.md` 갱신 노트. CONTRACTS.md 변경 없음(봇 프로토콜 미변경). Issue #5 |
| 2026-08-11 | (커밋 전) | SeungJoon (Claude Code) | 대회 규칙 | 설정 드롭다운에 "서브권" 하위메뉴 추가 (랜덤/승자/패자, 랜덤이 기본). `SERVE_RULE`에 `LOSER` 신규 — 점수를 잃은 쪽이 서브를 가져가므로 뒤처진 쪽에 공이 간다. `ui.js`의 기존 옵션 패턴 그대로 배선(applyOptions/saveOptions/loadOptions/setSelectedOptionsBtn/설정 초기화 + 하위메뉴 표시), localStorage 키는 `pv-offline-serveRule`. 저장값이 세 규칙 중 하나가 아니면 무시하고 현재 규칙 유지. 경기 중 변경 시 다음 득점부터 적용되며, 승리점수와 달리 "이미 지나쳤는지" 가드가 필요 없음. `pikavolley.js`는 `ui.js`를 import하지 않아 순환 참조 없음 | 없음 |
| 2026-08-11 | (커밋 전) | SeungJoon (Claude Code) | 대회 규칙 | PR #9 리뷰 반영 — 운영자/터치 제한으로 점수가 날 때 서브권이 갱신되지 않던 버그 수정. `round()`는 매 득점마다 서브권을 다시 뽑는데 `forceNextRound()`에는 해당 처리가 없어, 그 경로로 난 점수는 서브가 직전 값 그대로 유지됐음(봇 대결처럼 터치 제한이 자주 걸리면 경기 내내 한쪽 서브로 고정된 것처럼 보임). 두 경로를 성격에 맞게 분리: 터치 제한은 일반 득점이므로 `decideNextServe()`를 그대로 타고, 운영자 버튼은 랜덤이 아니라 심판이 직접 지정하도록 "다음 랠리로"를 왼쪽 서브/오른쪽 서브 두 버튼으로 분기(3개 로케일 마크업·안내 문구 갱신). 브라우저에서 버튼별 서브 위치와 터치 제한 반복 시 서브 분포 확인 | 없음 |
| 2026-08-11 | 9385b44 | SeungJoon (Claude Code) | 대회 규칙 | 자기 진영 연속 터치 제한 추가 (`rules/touchLimit.js`). 한 진영이 공을 네트 너머로 보내지 않고 `MAX_TOUCHES_PER_SIDE`(=5)회 접촉하면 상대에게 1점. 원작은 자기 진영 터치 횟수에 제한이 없어 잘 짠 봇끼리 랠리가 무한정 길어질 수 있음. 접촉은 `isCollisionWithBallHappened`의 false→true 전이로 세고(게이지와 동일 패턴), 리셋 조건은 "상대가 쳤을 때"가 아니라 **공이 네트를 넘었을 때** — 네트를 맞고 같은 진영으로 되돌아오는 경우를 놓치지 않기 위함. 점수 부여는 운영자 콘솔의 `awardPoint()`를 재사용해 수동/자동 경로를 일원화. 엔진 파일 무수정. `main.js`에서 `start()` 이후에 배선(ticker 콜백이 등록 순서대로 실행되므로 `gameLoop()` 뒤에 관찰해야 함). 브라우저에서 전용 테스트 봇으로 5회 접촉 시 실점·랠리 종료 확인 | 없음. 반칙 사유를 화면에 표시하지 않기로 결정(원작에 해당 메시지 에셋 없음) — 에셋 준비되면 후속 |
| 2026-08-11 | 56143b5 | SeungJoon (Claude Code) | 대회 운영 | 운영자 콘솔 추가 (`operator/console.js` + 3개 로케일 마크업/CSS/`main.js` 배선). 메뉴바 "운영자" 버튼 → 패널에서 좌우 점수 ±1, 다음 랠리로, 점수 초기화. 점수 편집과 랠리 진행을 별도 버튼으로 분리 — 중단된 경기 복구는 +1을 여러 번 눌러야 하는데 그때마다 랠리가 끝나면 안 되기 때문. `awardPoint(side)`가 둘을 합친 형태로, Phase 4-B(자기 진영 터치 제한)가 이걸 재사용할 예정. 엔진 파일 무수정(assembly layer). 브라우저 확인 중 발견한 3건 수정: 긴 라벨이 고정폭 `btn-in-box`에서 두 줄로 잘리던 것, 승리점수 초과로 점수가 계속 오르던 것(0~winningScore 클램프 + `+1` 비활성화), 경기 종료를 `−1`로 되돌렸을 때 "GAME END" 스프라이트가 화면에 남던 것(`drawGameEndMessage`가 켜기만 하고 끄지 않으며 원작은 GameView를 숨길 때만 정리함) | 없음. 승리점수 도달 시 승/패 모션과 8.4초 후 인트로 복귀는 원작 동작 유지로 결정 |
| 2026-08-11 | 5def765 | SeungJoon (Claude Code) | 빌드 | `npm run build`가 develop에서 항상 실패하던 것 수정. `webpack.common.js`의 pyodide 복사 패턴이 절대경로 + `'/*'` 조합이라 윈도우에서 구분자가 섞이고(`C:\...\pyodide/*`), globber가 백슬래시를 이스케이프로 읽어 매칭이 0건 → CleanWebpackPlugin이 `dist`를 이미 지운 뒤라 산출물이 하나도 안 나왔음. 파일 내 다른 패턴들과 같은 방식(`context` + 순수 glob `'*'`)으로 교체. 수정 후 `dist` 57개 파일 정상 생성, `dist/pyodide/` 6개(mjs/wasm/stdlib zip/lock json) 복사 확인 | 없음. 참고: 수정 후 workbox가 `pyodide.asm.wasm`(9.6MB)·`python_stdlib.zip`(2.55MB)을 precache에서 제외한다는 경고 발생 → 오프라인 환경에서 Python 봇이 동작하려면 별도 검토 필요(ADR-0014의 미해결 항목과 연결) |
| 2026-08-11 | 5b60625 | SeungJoon (Claude Code) | 대회 규칙 | 서브권을 승자 서브 → 랜덤 서브로 변경(대회 기본값). `pikavolley.js`에 `SERVE_RULE`(WINNER/RANDOM) enum + `decideNextServe()` 추가하고 서브권 결정 3개 지점(경기 시작 첫 서브, 양측 득점 시)을 이 메서드로 교체. `Math.random`이 아니라 원작 `rand()`를 쓰므로 `setCustomRng`로 시드를 고정하면 서브까지 재현됨. `serveRule`만 바꾸면 원작 규칙으로 되돌아감. 브라우저에서 랜덤 분포 및 WINNER 복원 동작 확인. 봇 스냅샷 `meta.isPlayer2Serve`는 의미/타입 불변이라 CONTRACTS.md 변경 없음 | 없음 (CONTRACTS 변경 불필요). 후속: `develop-skill`의 `skill/gauge.js` `GAUGE_ON_SERVE=0`이 "승자 서브" 전제라 재검토 필요 |
| 2026-08-10 | (커밋 전) | woochan (Claude Code) | ④ 스킬/에셋 | 에셋 해상도 스케일링 1단계 — `assets_path.js`에 `RATIO` 상수 도입(값 1), `main.js` 렌더러 크기와 `view.js` 전 좌표에 적용. RATIO=1이라 렌더링 결과는 기존과 동일하며, 배선 정확성만 검증(브라우저 육안 확인 완료). 텍스처 크기(`texture.width` 등), `anchor`, 좌우반전용 `scale.x`, 타일 개수는 의도적으로 제외. 후속: RATIO 값 확정 + 고해상도 에셋 제작 | DECISIONS.md ADR-0019 신규 등재(OPEN) |
| 2026-08-05 | (커밋 전) | 팀장 (Claude Code) | ⑤ 다국어 봇 | Phase 5 구현 1차 완료 (Python 러너). `TICK_FRAME_GROUP_SIZE=3` 반영, `pyodide@314.0.3` 설치 + `copy-webpack-plugin`으로 `dist/pyodide/`에 정적 복사, `botContract.js`에 `BOT_LANGUAGE` enum 추가, `botWorkerPython.js` 신설(Pyodide + numpy 자동 로드, 4단계 진행 phase 이벤트), `PikaBotInput`에 `language` 파라미터 + Worker URL 분기, `botWorker.js`도 phase 필드로 통일, `testSetup.js`에 언어 선택 UI + 진행 상태 문구, 3개 로케일 HTML/CSS 마크업 추가, `exampleBots.js`에 Python 추격형 예시 추가, `04-bot-writing-guide.md`에 Python 절/공통 실패 처리 절/타이밍 갱신 반영. `npm run verify` 통과. numpy wheel은 로컬 미포함 — 첫 로드 시 Pyodide CDN에서 fallback (offline 완전 지원은 후속 ADR) | 코드: `src/resources/js/bot/*`, `webpack.common.js`, `package.json`(pyodide 314.0.3 pin), `src/{en,ko,zh}/index.html`, `src/resources/style.css`. 문서: `docs/architecture/04-bot-writing-guide.md`, ADR-0014 numpy CDN fallback 메모 추가 |
| 2026-08-05 | (커밋 전) | 팀장 (Claude Code) | ⑤ 다국어 봇 | Phase 5(다국어 봇 지원) stub 확정 — Python 우선. 브라우저 내 WASM(Pyodide) 방식으로 확정, `TICK_FRAME_GROUP_SIZE`를 1→3으로 상향(Pyodide 오버헤드 흡수 + 매프레임 결정으로 인한 승부 결착 어려움 완화). ADR 7건 신규 등재, CONTRACTS.md에 §1.4(언어별 진입점) 신설 및 §1.3/§2 갱신. 아직 코드 변경 없음(문서 단위 커밋) | CONTRACTS.md v0.5, DECISIONS.md ADR-0012~0018 신규 RESOLVED, ADR-0001 SUPERSEDED(→0016), PHASES.md Phase 5 신설 |
| 2026-07-24 | (커밋 전) | 팀장 (Claude Code) | ② 테스트 환경 | 사용자 제보 버그 수정: 좌우 둘 다 "봇 코드"로 설정하면 첫 경기 후 인트로/메뉴로 못 돌아옴(봇이 keyboardArray를 계속 점유해서 사람이 다음 경기를 내비게이션 못 함). "라운드 시작 시 1회 적용"을 "매 틱 게임 상태와 동기화하는 상시 워처"로 변경 — 경기 중(round/afterEndOfRound/beforeStartOfNextRound)에만 봇/AI 적용, 그 외(인트로/메뉴)엔 자동으로 실키보드 복원. Playwright로 restart() 후 키 입력으로 메뉴 진입 + 2차 라운드 봇 재적용까지 확인. 겸사겸사 결정 로그를 ADR 파일 방식(`docs/agent-dev/decisions/ADR-NNNN-*.md`)으로 분리, 브랜치 전략(main/develop/feature)+GitHub Issue 연동을 AGENTS.md/KICKOFF.md에 반영, 팀 온보딩 문서(`TEAM_ONBOARDING.md`) 작성 | DECISIONS.md ADR-0001~0011로 재구성, ADR-0011 신규 |
| 2026-07-19 | (커밋 전) | 팀장 (Claude Code) | ② 테스트 환경 | Phase 2 구현 완료: 좌/우 진영을 [키보드\|봇 코드\|기본 AI] 중 자유롭게 조합하는 Bot Setup 패널 추가 (`bot/testSetup.js`, `bot/nullInput.js`, `botInput.js`에 `onInitResult` 추가, `en/ko/zh index.html`+`style.css`). `devBotHook.js` 삭제. 구현 중 발견한 이중 리스너 등록 버그(D-010: config mutate + 중복 arm으로 봇 코드가 빈 채로 로드됨)를 계측 후 수정. Playwright로 키보드-봇/기본AI-봇/봇-봇 세 조합과 새로고침 후 설정 자동 재적용까지 실제 브라우저에서 검증 | DECISIONS.md D-010 RESOLVED, PHASES.md Phase 2 완료 |
| 2026-07-11 | (커밋 전) | 팀장 (Claude Code) | ① 입력 구조 | Phase 1 구현 완료: `bot/botContract.js`(스냅샷 빌더/상수), `bot/botWorker.js`(Worker 실행기), `bot/botInput.js`(`PikaBotInput`), 임시 dev 와이어링 `bot/devBotHook.js` + `main.js` 연결. 구현 중 D-009(메인 스레드가 Worker 응답을 그 틱 안에 동기 대기 불가) 발견·반영. `npm run lint`/`build` 통과, Playwright로 실제 브라우저에서 `?bot=right` 기동 → 봇이 자율적으로 공을 쫓아가 점프+파워히트(state 0→1→2)하는 것을 스크린샷/상태값으로 확인 | CONTRACTS.md v0.4, DECISIONS.md D-009 RESOLVED(확인 요망), PHASES.md Phase 1 완료 |
| 2026-07-10 | (커밋 전) | 팀장 (Claude Code) | - | D-004(제출 인프라: 이메일+수동 로드 가정) / D-005(현재 스레드 기준 오너십: 팀장+Claude Code) 결정 및 반영. DECISIONS.md OPEN 항목 소진, AGENTS.md §4 담당자 표 채움 | DECISIONS.md D-004/D-005 RESOLVED, AGENTS.md §4 |
| 2026-07-07 | (커밋 전) | Claude Code (사용자 승인) | ① 입력 구조 | D-001(틱 그룹핑=1 확정)/D-002(타임아웃·오반환 시 무입력)/D-003(Worker 격리) 결정 및 반영. CONTRACTS.md에 §1.3(실행 모델) 신설 | CONTRACTS.md v0.3, DECISIONS.md D-001/D-002/D-003 RESOLVED |
| 2026-07-07 | (커밋 전) | YoonJinJung (Claude Code) | ① 입력 구조 | 브레인스토밍한 봇 프로토콜을 엔진 코드와 대조 검토. 출력을 4-bit(left/right/jump/spike)→엔진과 1:1인 3필드(x/y/hit)로 변경, 좌표계는 원본 정수 픽셀 그대로 확정, `expectedLandingPointX` 노출 확정, 세트제 미도입 확정 | CONTRACTS.md v0.2, DECISIONS.md D-006/D-007/D-008 RESOLVED |
| 2026-07-07 | (초기 세팅, 커밋 전) | YoonJinJung (Claude Code) | - | agent 개발 환경 문서 인프라 구성 (CLAUDE.md/AGENTS.md 이중화, CONTRACTS/PHASES/KICKOFF/VERIFY/TRACKER/DECISIONS) | CONTRACTS.md v0.1 |
